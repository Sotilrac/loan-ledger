default:
  image: node:24-bookworm
  interruptible: true

variables:
  PNPM_HOME: '$CI_PROJECT_DIR/.pnpm-store'
  npm_config_cache: '$CI_PROJECT_DIR/.npm'

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

stages:
  - install
  - check
  - build
  - deploy

.pnpm-cache: &pnpm-cache
  key:
    files:
      - pnpm-lock.yaml
  paths:
    - .pnpm-store/
    - node_modules/
    - packages/*/node_modules/

.setup: &setup
  before_script:
    - corepack enable
    - corepack prepare pnpm@10.33.1 --activate
    - pnpm config set store-dir .pnpm-store
    - pnpm install --frozen-lockfile

install:
  stage: install
  cache:
    <<: *pnpm-cache
    policy: pull-push
  <<: *setup
  script:
    - pnpm -v
    - node -v
    - echo "Dependencies installed."

lint:
  stage: check
  needs: [install]
  cache:
    <<: *pnpm-cache
    policy: pull
  <<: *setup
  script:
    - pnpm lint

typecheck:
  stage: check
  needs: [install]
  cache:
    <<: *pnpm-cache
    policy: pull
  <<: *setup
  script:
    - pnpm typecheck

test:
  stage: check
  needs: [install]
  cache:
    <<: *pnpm-cache
    policy: pull
  <<: *setup
  script:
    - pnpm test
  coverage: '/Statements\s+:\s+(\d+\.\d+)%/'

# PHP-side checks for the Nextcloud app. Runs in its own image so the JS
# pipeline above doesn't pay PHP install costs. Job is skipped on commits
# that don't touch the Nextcloud package.
.nc-php-base: &nc-php-base
  image: php:8.2-cli
  before_script:
    - apt-get update -q
    - apt-get install -y -q git unzip libzip-dev
    - docker-php-ext-install zip
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
    - cd packages/nextcloud
    - composer install --no-progress
  rules:
    - if: $CI_COMMIT_BRANCH
      changes:
        - packages/nextcloud/**/*
        - .gitlab-ci.yml
    - if: $CI_COMMIT_TAG

ci-nextcloud-php-test:
  stage: check
  needs: []
  <<: *nc-php-base
  script:
    - composer test

ci-nextcloud-php-lint:
  stage: check
  needs: []
  <<: *nc-php-base
  script:
    - composer cs:check
    - composer psalm

build:
  stage: build
  needs: [lint, typecheck, test]
  cache:
    <<: *pnpm-cache
    policy: pull
  <<: *setup
  script:
    - pnpm build
  artifacts:
    name: 'web-dist-$CI_COMMIT_SHORT_SHA'
    paths:
      - packages/web/dist/
    expire_in: 14 days

# Deploy the web bundle to Cloudflare Pages on every push to the default
# branch. Requires CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID as masked
# CI variables.
deploy:cloudflare:
  stage: deploy
  needs: [build]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  cache:
    <<: *pnpm-cache
    policy: pull
  <<: *setup
  script:
    - pnpm run deploy:pages

# Build a signed Nextcloud App Store tarball on `nc-vX.Y.Z` tags, attach it
# to the GitLab release as an asset, then submit the release to the App
# Store API. Requires two CI variables:
#   - NC_SIGNING_KEY    (file): the private key paired with your registered cert
#   - NC_APPSTORE_TOKEN (variable, masked): API token from
#       https://apps.nextcloud.com/account/token. Token auth is required for
#       accounts that sign in via GitHub (no password to use for basic auth).
release:nextcloud:
  stage: deploy
  rules:
    - if: $CI_COMMIT_TAG =~ /^nc-v\d+\.\d+\.\d+$/
  image: node:24-bookworm
  variables:
    APP_VERSION: '${CI_COMMIT_TAG#nc-v}'
    OUT_DIR: dist
  before_script:
    - apt-get update -q
    - apt-get install -y -q php-cli php-xml php-zip php-mbstring composer rsync libxml2-utils curl jq openssl
    - corepack enable
    - corepack prepare pnpm@10.33.1 --activate
    - pnpm install --frozen-lockfile
    # --no-scripts: the post-install-cmd runs the bamarni bin plugin, a dev-only
    # dep that isn't present under --no-dev. --working-dir avoids cd so later
    # steps keep resolving paths from the repo root.
    - composer install --no-progress --no-dev --no-scripts --working-dir=packages/nextcloud
    - pnpm --filter @loan-ledger/nextcloud build
  script:
    # Validate info.xml against the live App Store schema before packaging.
    - curl -s https://apps.nextcloud.com/schema/apps/info.xsd -o /tmp/info.xsd
    - xmllint --schema /tmp/info.xsd packages/nextcloud/appinfo/info.xml --noout
    # Stage runtime files and pack.
    - mkdir -p $OUT_DIR/loanledger
    - rsync -a --exclude-from=packages/nextcloud/.nextcloudignore
      --exclude=node_modules --exclude=tests --exclude=src
      packages/nextcloud/ $OUT_DIR/loanledger/
    - TAR_NAME="loanledger-${APP_VERSION}.tar.gz"
    - tar -czf "$OUT_DIR/$TAR_NAME" -C "$OUT_DIR" loanledger
    - SIG=$(openssl dgst -sha512 -sign "$NC_SIGNING_KEY" "$OUT_DIR/$TAR_NAME" | base64 -w0)
    - echo "$SIG" > "$OUT_DIR/$TAR_NAME.sig"
    # The App Store fetches `download` to verify the signature, so the tarball
    # must be publicly reachable *before* the POST. The GitLab `release:` below
    # only exists after the script, so publish to the generic Package Registry
    # (anonymously downloadable on this public project) and submit that URL.
    - PKG_URL="${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/loanledger/${APP_VERSION}/${TAR_NAME}"
    - |
      curl -fsS --header "JOB-TOKEN: $CI_JOB_TOKEN" \
        --upload-file "$OUT_DIR/$TAR_NAME" "$PKG_URL"
    - |
      curl -fsS -X POST -H 'Content-Type: application/json' \
        -H "Authorization: Token ${NC_APPSTORE_TOKEN}" \
        https://apps.nextcloud.com/api/v1/apps/releases \
        -d "$(jq -n --arg url "$PKG_URL" --arg sig "$SIG" '{download: $url, signature: $sig, nightly: false}')"
  artifacts:
    name: 'loanledger-$CI_COMMIT_TAG'
    paths:
      - $OUT_DIR/loanledger-*.tar.gz
      - $OUT_DIR/loanledger-*.tar.gz.sig
    expire_in: never
